@page
@model RazorPage.Pages.Dashboard.AppointmentsModel
@{
    ViewData["Title"] = "Appointments";
}

<h1>Appointments</h1>

<!-- You might include search/filter form here -->
<form method="get" class="mb-3">
    <div class="form-group">
        <label for="nameSearch">Search by Name:</label>
        <input type="text" name="nameSearch" id="nameSearch" class="form-control" value="@Request.Query["nameSearch"]" />
    </div>
    <button type="submit" class="btn btn-primary">Search</button>
</form>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>User</th>
            <th>Appointment Date</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appointment in Model.AppointmentPage.Items)
        {
            <tr>
                <td>@appointment.Name</td>
                <td>@appointment.UserName</td>
                <td>@appointment.AppointmentDate.ToString("g")</td>
                <td>@appointment.Status</td>
            </tr>
        }
    </tbody>
</table>

<!-- Basic pagination controls -->
<nav>
    <ul class="pagination">
        @if (Model.AppointmentPage.PageNumber > 1)
        {
            <li class="page-item">
                <a class="page-link" href="?index=@(Model.AppointmentPage.PageNumber - 1)&pageSize=@Model.AppointmentPage.PageSize">Previous</a>
            </li>
        }
        <li class="page-item active">
            <span class="page-link">@Model.AppointmentPage.PageNumber</span>
        </li>
        @if (Model.AppointmentPage.PageNumber * Model.AppointmentPage.PageSize < Model.AppointmentPage.TotalCount)
        {
            <li class="page-item">
                <a class="page-link" href="?index=@(Model.AppointmentPage.PageNumber + 1)&pageSize=@Model.AppointmentPage.PageSize">Next</a>
            </li>
        }
    </ul>
</nav>
