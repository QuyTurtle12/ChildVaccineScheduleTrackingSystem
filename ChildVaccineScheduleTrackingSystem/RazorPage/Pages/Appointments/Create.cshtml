@page
@model RazorPage.Pages.Appointments.CreateModel
@{
    ViewData["Title"] = "Đặt lịch hẹn - Trung tâm Vaccine";
}



<!-- Nội dung chính -->
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card lively-card animate__animated animate__fadeIn">
                <div class="card-body">
                    <h1 class="text-center text-vibrant mb-4 animate__animated animate__bounceIn">
                        <i class="fas fa-calendar-plus mr-2"></i>Đặt lịch hẹn
                    </h1>
                    <form method="post" id="createAppointmentForm">
                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger d-none" id="errorMessages">
                                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                                {
                                    <p>@error.ErrorMessage</p>
                                }
                            </div>
                        }
                        <input type="hidden" asp-for="Appointment.UserId" />
                        @if (Model.UserRole == "Staff")
                        {
                            <div class="form-group">
                                <label asp-for="Appointment.CustomerPhoneNumber" class="font-weight-bold">Số điện thoại khách hàng</label>
                                <div class="input-group">
                                    <div class="input-group-prepend">
                                        <span class="input-group-text"><i class="fas fa-phone"></i></span>
                                    </div>
                                    <input type="text" id="customerPhone" asp-for="Appointment.CustomerPhoneNumber" class="form-control" placeholder="Nhập số điện thoại khách hàng" />
                                </div>
                                <span asp-validation-for="Appointment.CustomerPhoneNumber" class="text-danger"></span>
                            </div>
                        }
                        <div class="form-group">
                            <label asp-for="Appointment.AppointmentDate" class="font-weight-bold">Ngày hẹn</label>
                            <div class="input-group">
                                <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="fas fa-calendar-alt"></i></span>
                                </div>
                                <input asp-for="Appointment.AppointmentDate" class="form-control" type="datetime-local" />
                            </div>
                            <span asp-validation-for="Appointment.AppointmentDate" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Appointment.Name" class="font-weight-bold">Tên cuộc hẹn</label>
                            <div class="input-group">
                                <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="fas fa-tag"></i></span>
                                </div>
                                <input asp-for="Appointment.Name" class="form-control" placeholder="Nhập tên cuộc hẹn" />
                            </div>
                            <span asp-validation-for="Appointment.Name" class="text-danger"></span>
                        </div>
                        <div class="form-group" style="display: none">
                            <input asp-for="Appointment.Status" class="form-control" value="0" />
                        </div>
                        <div class="text-center mt-4">
                            <button type="submit" class="btn btn-success btn-lg animate__animated animate__pulse animate__infinite">
                                <i class="fas fa-save mr-2"></i>Đặt lịch
                            </button>
                            <a class="btn btn-secondary ml-2" asp-page="./Index"><i class="fas fa-arrow-left mr-2"></i>Quay lại</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>



@section Scripts {
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const form = document.getElementById("createAppointmentForm");
            const customerPhoneInput = document.getElementById("customerPhone");
            const errorMessagesDiv = document.getElementById("errorMessages");

            if (!errorMessagesDiv) {
                console.error("Error: The div with id 'errorMessages' is missing.");
                return;
            }

            form.addEventListener("submit", function (event) {
                errorMessagesDiv.innerHTML = "";
                errorMessagesDiv.classList.add("d-none");

                let errors = [];

                if ('@Model.UserRole' === 'Staff' && customerPhoneInput && customerPhoneInput.value.trim() === "") {
                    errors.push("Số điện thoại khách hàng là bắt buộc khi đặt lịch.");
                }

                if (errors.length > 0) {
                    event.preventDefault();
                    errorMessagesDiv.innerHTML = errors.join("<br>");
                    errorMessagesDiv.classList.remove("d-none");
                }
            });
        });
    </script>
}

<style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #f0f4f8;
    }

    .navbar {
        background-color: #0078D4 !important;
        color: white;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .navbar-brand, .nav-link {
        color: white !important;
        transition: color 0.3s;
    }

        .nav-link:hover {
            color: #F28C38 !important;
        }

    .lively-card {
        border: none;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s, box-shadow 0.3s;
        background-color: white;
        border-radius: 10px;
    }

        .lively-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }

    .text-vibrant {
        color: #F28C38;
        font-weight: bold;
    }

    .btn-success {
        background-color: #28A745;
        border: none;
        transition: transform 0.2s;
    }

        .btn-success:hover {
            background-color: #218838;
            transform: scale(1.05);
        }

    .input-group-text {
        background-color: #0078D4;
        color: white;
    }

    .form-control:focus {
        border-color: #28A745;
        box-shadow: 0 0 5px rgba(40, 167, 69, 0.5);
    }

    .footer {
        background-color: #0078D4;
        color: white;
        padding: 20px 0;
        text-align: center;
    }
</style>